# Database
DATABASE_URL=postgresql://betterworld:betterworld_dev@localhost:5432/betterworld

# Redis
REDIS_URL=redis://localhost:6379

# API Server
API_PORT=4000
NODE_ENV=development
LOG_LEVEL=info

# Auth
JWT_SECRET=your-jwt-secret-min-16-chars

# CORS
CORS_ORIGINS=http://localhost:3000

# Ed25519 Heartbeat Signing Keys (base64-encoded PEM)
# Generate with: node -e "const c=require('crypto');const{publicKey,privateKey}=c.generateKeyPairSync('ed25519');console.log('BW_HEARTBEAT_PRIVATE_KEY='+privateKey.export({type:'pkcs8',format:'pem'}).toString('base64'));console.log('BW_HEARTBEAT_PUBLIC_KEY='+publicKey.export({type:'spki',format:'pem'}).toString('base64'))"
BW_HEARTBEAT_PRIVATE_KEY=
BW_HEARTBEAT_PUBLIC_KEY=

# Email (Resend) — optional for dev, codes logged to console if not set
RESEND_API_KEY=
