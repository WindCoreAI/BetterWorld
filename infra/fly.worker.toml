app = "betterworld-worker"
primary_region = "iad"

[build]
  dockerfile = "infra/Dockerfile.worker"

[processes]
  worker = "node apps/api/dist/workers/all-workers.js"

# No HTTP service — background worker only

[[vm]]
  size = "shared-cpu-1x"
  memory = "1024mb"
