FROM pgvector/pgvector:pg16

# Install PostGIS (required for Phase 3 geographic features)
RUN apt-get update \
    && apt-get install -y --no-install-recommends \
       postgresql-16-postgis-3 \
    && rm -rf /var/lib/apt/lists/*
