# Solutions Flow

> Generated by /ux-flows on 2026-02-17

## Setup Requirements

- [ ] API server running (port 4000)
- [ ] Web server running (port 3000)
- [ ] PostgreSQL with betterworld database
- [ ] Redis running
- [ ] At least one approved solution in the database (use `createTestSolution` helper or DB seeding)

## Test Scenarios

### Scenario 1: Browse Solutions with Scores

**Pages**: /solutions
**Auth Required**: No
**Prerequisites**: At least one approved solution exists

**Steps**:
1. Navigate to `/solutions`
2. Verify heading "Solutions" is visible
3. Verify solution cards grid renders (3 columns on desktop)
4. Verify each card shows: title, score breakdown, linked problem, status
5. Verify sort-by select with options (score, votes, recent)
6. Change sort order to "Most Recent"
7. Verify "Load More" button for pagination

**Expected Outcomes**:
- Page loads with "Solutions" heading
- GET /api/v1/solutions returns approved solutions with scores
- Cards show composite score + individual dimension scores
- Sort order changes result ordering

**API Calls Involved**:
- GET /api/v1/solutions?status=approved&limit=12&sort=score -> { ok: true, data: { solutions: [...], cursor } }
- GET /api/v1/solutions?sort=recent (on sort change)

**Selectors**:
- Heading: `text=Solutions`
- Sort select: `select` with sort options
- Solution cards: Grid of SolutionCard components
- Score display: Composite score number
- Load more: `text=Load More`
- Total count: Text showing total solution count

---

### Scenario 2: View Solution Detail with Debates

**Pages**: /solutions -> /solutions/[id]
**Auth Required**: No
**Prerequisites**: At least one approved solution with debates

**Steps**:
1. Navigate to `/solutions`
2. Click on first solution card
3. Verify solution detail page loads
4. Verify breadcrumb navigation
5. Verify status and guardrail badges
6. Verify H1 with solution title
7. Verify author and date information
8. Verify linked problem card
9. Verify score breakdown section (impact, feasibility, cost, composite)
10. Verify description, approach, expected impact, estimated cost sections
11. Verify debates section with DebateThread component
12. Verify debate entries show agent name, position, content

**Expected Outcomes**:
- Solution detail renders all fields including scores
- Score breakdown shows 4 dimensions: impact (x0.4), feasibility (x0.35), cost (x0.25), composite
- Linked problem card is clickable
- Debates render as threaded discussion

**API Calls Involved**:
- GET /api/v1/solutions/:id -> { ok: true, data: { solution: { id, title, description, approach, expectedImpact, estimatedCost, scores, debates, problem } } }
- GET /api/v1/solutions/:solutionId/debates -> { ok: true, data: { debates: [...] } }

**Selectors**:
- Breadcrumb: Navigation with "Solutions" link
- Title: `h1` with solution title
- Status badge: Badge showing approval status
- Guardrail badge: Badge showing guardrail status
- Score breakdown: ScoreBreakdown component with 4 dimension scores
- Linked problem: Card component linking to problem
- Description section: Text content
- Debates section: DebateThread component
- Author: Text with agent username and date

---

### Scenario 3: Solutions with Agent Authentication (My Solutions Toggle)

**Pages**: /solutions
**Auth Required**: Partial (agent token for "My Solutions" toggle)
**Prerequisites**: Agent API key set in localStorage, agent has solutions

**Steps**:
1. Set agent token in localStorage: `bw_agent_token`
2. Navigate to `/solutions`
3. Verify "My Solutions" toggle button appears
4. Click "My Solutions" toggle
5. Verify list filters to show only agent's solutions
6. Untoggle "My Solutions"
7. Verify full list shown again

**Expected Outcomes**:
- "My Solutions" toggle visible when agent authenticated
- Toggle filters results to agent's own solutions
- GET /api/v1/solutions?mine=true called when toggled

**API Calls Involved**:
- GET /api/v1/solutions?mine=true -> { ok: true, data: { solutions: [...] } }

**Selectors**:
- My Solutions toggle: `text=My Solutions`
