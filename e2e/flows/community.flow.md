# Community Flow

> Generated by /ux-flows on 2026-02-17

## Setup Requirements

- [ ] API server running (port 4000)
- [ ] Web server running (port 3000)
- [ ] PostgreSQL with betterworld database
- [ ] Redis running

## Test Scenarios

### Scenario 1: Domain Directory Browsing

**Pages**: /domains
**Auth Required**: No
**Prerequisites**: None (domain data sourced from constants)

**Steps**:
1. Navigate to `/domains`
2. Verify heading "Domain Communities" is visible
3. Verify grid of 15 DomainCard components
4. Verify each card shows domain name, metrics (problem count, solution count, etc.)
5. Click on first domain card
6. Expect navigation to `/domains/{slug}`

**Expected Outcomes**:
- Page loads with "Domain Communities" heading
- 15 domain cards rendered in grid
- Each card shows aggregated metrics
- Cards link to individual domain pages

**API Calls Involved**:
- GET /api/v1/domains -> { ok: true, data: { domains: [...] } }

**Selectors**:
- Heading: `text=Domain Communities`
- Domain cards: Grid of DomainCard components
- Card metrics: Problem count, solution count, contributor count
- Card link: Links to `/domains/{slug}`

---

### Scenario 2: Domain Community Page

**Pages**: /domains/[slug]
**Auth Required**: No
**Prerequisites**: None

**Steps**:
1. Navigate to `/domains/education_access` (or any valid domain slug)
2. Verify domain name heading is visible
3. Verify active milestone banner (MilestoneBanner, if milestone active with 7-day window)
4. Verify domain metrics section (DomainMetrics component)
5. Verify top contributors section (DomainContributors component)
6. Verify monthly highlights section (DomainHighlights component)
7. Verify milestone timeline (MilestoneTimeline component)
8. Verify community intelligence section (CommunityIntelligence component)

**Expected Outcomes**:
- Domain community page loads with full data
- Metrics show problem/solution/mission counts
- Top contributors ranked by reputation
- Monthly highlights show recent activity
- Active milestones displayed with celebration banners

**API Calls Involved**:
- GET /api/v1/domains/education_access -> { ok: true, data: { domain, metrics } }
- GET /api/v1/domains/education_access/contributors -> { ok: true, data: { contributors: [...] } }
- GET /api/v1/domains/education_access/highlights -> { ok: true, data: { highlights: [...] } }
- GET /api/v1/domains/education_access/milestones -> { ok: true, data: { milestones: [...] } }
- GET /api/v1/intelligence/latest -> { ok: true, data: { report } }

**Selectors**:
- Domain heading: `h1` with domain display name
- Milestone banner: MilestoneBanner component (7-day celebration)
- Metrics: DomainMetrics component with counts
- Contributors: DomainContributors list with avatars and reputation
- Highlights: DomainHighlights section
- Timeline: MilestoneTimeline component
- Intelligence: CommunityIntelligence section

---

### Scenario 3: City Chapter Page

**Pages**: /city -> /city/[city]
**Auth Required**: No
**Prerequisites**: None

**Steps**:
1. Navigate to `/city`
2. Verify heading "City Dashboards" is visible
3. Verify grid of city cards (Portland, Chicago, Denver)
4. Click on "Portland" card
5. Verify city dashboard loads at `/city/portland`
6. Verify city name heading
7. Verify stats cards (Total Problems, Observations, Local Validators, Avg Resolution)
8. Verify problems-by-category bar chart
9. Verify Leaflet heatmap (CityHeatmap component)

**Expected Outcomes**:
- City selector page shows all supported cities
- City dashboard loads with metrics and heatmap
- Stats cards show aggregated city data
- Heatmap renders problem density

**API Calls Involved**:
- GET /api/v1/city -> { ok: true, data: { cities: [...] } }
- GET /api/v1/city/portland/metrics -> { ok: true, data: { metrics } }

**Selectors**:
- City selector heading: `text=City Dashboards`
- City cards: Grid cards with city names and problem counts
- Portland link: `a[href="/city/portland"]`
- City name heading: `h1` with city name
- Stats cards: Cards showing Total Problems, Observations, Local Validators, Avg Resolution
- Bar chart: Problems-by-category visualization
- Heatmap: CityHeatmap Leaflet component

---

### Scenario 4: Leaderboards with Filters

**Pages**: /leaderboards
**Auth Required**: No
**Prerequisites**: At least some reputation data in the database

**Steps**:
1. Navigate to `/leaderboards`
2. Verify heading "Leaderboards" is visible
3. Verify LeaderboardTypeSwitcher component (reputation type tabs)
4. Verify LeaderboardFilters component (period + domain filters)
5. Verify LeaderboardTable renders entries
6. Switch leaderboard type (e.g., from reputation to another type)
7. Apply period filter (e.g., "This Week")
8. Apply domain filter (e.g., "Education Access")
9. Verify table updates with filtered data

**Expected Outcomes**:
- Leaderboard page loads with ranked entries
- Type switcher changes leaderboard category
- Period and domain filters narrow results
- Table shows rank, name, score, tier badge

**API Calls Involved**:
- GET /api/v1/leaderboards?type=reputation&period=all -> { ok: true, data: { entries: [...] } }
- GET /api/v1/leaderboards?type=reputation&period=week&domain=education_access (filtered)

**Selectors**:
- Heading: `text=Leaderboards`
- Type switcher: LeaderboardTypeSwitcher component (tabs/buttons)
- Period filter: Select or buttons for time period
- Domain filter: Select for domain
- Table: LeaderboardTable with rows showing rank, name, score
- Entries: Rows with user name, score, tier badge

---

### Scenario 5: Community Intelligence (Public)

**Pages**: /domains/[slug] (intelligence section)
**Auth Required**: No
**Prerequisites**: At least one intelligence report generated (monthly worker)

**Steps**:
1. Navigate to `/domains/education_access`
2. Scroll to Community Intelligence section
3. Verify CommunityIntelligence component renders
4. Verify collective progress metrics
5. Verify domain trends data

**Expected Outcomes**:
- Intelligence section shows latest monthly report
- Collective progress and domain trends visible
- Public API returns report data

**API Calls Involved**:
- GET /api/v1/intelligence/latest -> { ok: true, data: { report: { collectiveProgress, domainTrends, ... } } }
- GET /api/v1/intelligence/history -> { ok: true, data: { reports: [...] } }

**Selectors**:
- Intelligence section: CommunityIntelligence component
- Progress metrics: Collective progress data
- Trends: Domain trend data
